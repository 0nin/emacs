(defun switch-on-elscreen()
    (interactive)
  (elscreen-start)
  )

(defun switch-on-ecb()
    (interactive)
  (ecb-activate)
  )

(defun switch-on-eide()
    (interactive)
  (eide-start)
    )

(provide 'custom-switch)
